<div class='o-grid__col o-grid__col--full'>

  <a href='{{ post.url | prepend: site.baseurl }}'
     class='c-post-card
            {% if forloop.first == true %} c-post-card--first {% endif %}
            {% if forloop.last == true %} c-post-card--last {% endif %}
            '
    >

    <h3 class='c-post-card__title'>{{ post.title }}{% if post.draft and jekyll.environment == "development" %}<span class="c-post-card__draft">[DRAFT]</span>{% endif %}</h3>

    {% if post.front_page_image %}
      <img src='{{ post.front_page_image | prepend: site.baseurl }}'
           class='c-post__image' alt='{{ post.title }}'>
    {% endif %}

    <span class='c-post-card__excerpt'>
      {% if post.excerpt %}
        {{ post.excerpt | markdownify }}
      {% else %}
        {{ post.content | strip_html | truncatewords:35 }}
      {% endif %}
    </span>

    {% comment %}
      <span class='c-post-card__date'>{{ post.date | date: '%d %b %Y' }}</span>
    {% endcomment %}
  </a>

</div>
